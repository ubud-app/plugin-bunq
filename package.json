{
	"name": "@dwimm/plugin-bunq",
	"description": "DWIMM Plugin for bunq, a clean alternative to traditional banking",
	"author": "Sebastian Pekarek <mail@sebbo.net>",
	"main": "lib/main.js",
	"scripts": {
		"check": "npm run check:eslint && npm run check:npm && npm run check:nsp",
		"check:quick": "npm run check:eslint",
		"check:eslint": "eslint ./",
		"check:nsp": "nsp check || true",
		"check:npm": "npm-check -s",
		"bump:reset": "npm --no-git-tag-version --force version $(npm view @dwimm/plugin-bunq version || echo 1.0.0)",
		"bump:production": "npm run bump:reset && npm --no-git-tag-version --force version from-git"
	},
	"dependencies": {
		"@bunq-community/bunq-js-client": "^0.27.0"
	},
	"devDependencies": {
		"eslint": "^5.8.0",
		"eslint-plugin-security": "^1.4.0",
		"npm-check": "^5.9.0",
		"nsp": "^3.2.1"
	}
}
